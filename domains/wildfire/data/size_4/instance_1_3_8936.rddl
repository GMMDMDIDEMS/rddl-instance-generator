non-fluents nf_wildfire_mdp__1_3 {
    domain = wildfire_mdp;
    objects {
        x_pos : {x1};
        y_pos : {y1,y2,y3};
    };
    non-fluents {
        NEIGHBOR(x1,y1,x1,y1);
        NEIGHBOR(x1,y1,x1,y2);
        NEIGHBOR(x1,y1,x1,y3);
        NEIGHBOR(x1,y2,x1,y1);
        NEIGHBOR(x1,y2,x1,y3);
        NEIGHBOR(x1,y3,x1,y2);
        TARGET(x1,y3);
    };
}

instance inst_wildfire_mdp__1_3 {
    domain = wildfire_mdp;
    non-fluents = nf_wildfire_mdp__1_3;
    init-state {
        burning(x1,y3);
        out-of-fuel(x1,y3);
    };
    max-nondef-actions = 1;
    horizon = 40;
    discount = 1.0;
}