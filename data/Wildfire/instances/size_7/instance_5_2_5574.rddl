non-fluents nf_wildfire_mdp__5_2 {
    domain = wildfire_mdp;
    objects {
        x_pos : {x1,x2,x3,x4,x5};
        y_pos : {y1,y2};
    };
    non-fluents {
        NEIGHBOR(x1,y1,x1,y1);
        NEIGHBOR(x1,y1,x1,y2);
        NEIGHBOR(x1,y1,x3,y1);
        NEIGHBOR(x1,y1,x5,y1);
        NEIGHBOR(x1,y1,x5,y2);
        NEIGHBOR(x1,y2,x1,y2);
        NEIGHBOR(x1,y2,x2,y2);
        NEIGHBOR(x1,y2,x3,y2);
        NEIGHBOR(x1,y2,x4,y1);
        NEIGHBOR(x1,y2,x4,y2);
        NEIGHBOR(x1,y2,x5,y2);
        NEIGHBOR(x2,y1,x2,y2);
        NEIGHBOR(x2,y1,x3,y1);
        NEIGHBOR(x2,y1,x4,y1);
        NEIGHBOR(x2,y1,x4,y2);
        NEIGHBOR(x2,y1,x5,y1);
        NEIGHBOR(x2,y2,x1,y1);
        NEIGHBOR(x2,y2,x1,y2);
        NEIGHBOR(x2,y2,x2,y1);
        NEIGHBOR(x2,y2,x2,y2);
        NEIGHBOR(x2,y2,x3,y2);
        NEIGHBOR(x2,y2,x4,y1);
        NEIGHBOR(x2,y2,x5,y1);
        NEIGHBOR(x3,y1,x1,y2);
        NEIGHBOR(x3,y1,x2,y1);
        NEIGHBOR(x3,y1,x2,y2);
        NEIGHBOR(x3,y1,x3,y1);
        NEIGHBOR(x3,y1,x3,y2);
        NEIGHBOR(x3,y1,x4,y1);
        NEIGHBOR(x3,y1,x4,y2);
        NEIGHBOR(x3,y1,x5,y2);
        NEIGHBOR(x3,y2,x1,y1);
        NEIGHBOR(x3,y2,x1,y2);
        NEIGHBOR(x3,y2,x3,y1);
        NEIGHBOR(x3,y2,x3,y2);
        NEIGHBOR(x3,y2,x4,y2);
        NEIGHBOR(x3,y2,x5,y1);
        NEIGHBOR(x3,y2,x5,y2);
        NEIGHBOR(x4,y1,x1,y1);
        NEIGHBOR(x4,y1,x1,y2);
        NEIGHBOR(x4,y1,x2,y1);
        NEIGHBOR(x4,y1,x2,y2);
        NEIGHBOR(x4,y1,x4,y1);
        NEIGHBOR(x4,y1,x5,y1);
        NEIGHBOR(x4,y1,x5,y2);
        NEIGHBOR(x4,y2,x1,y2);
        NEIGHBOR(x4,y2,x2,y1);
        NEIGHBOR(x4,y2,x2,y2);
        NEIGHBOR(x4,y2,x3,y1);
        NEIGHBOR(x4,y2,x3,y2);
        NEIGHBOR(x4,y2,x4,y1);
        NEIGHBOR(x4,y2,x5,y2);
        NEIGHBOR(x5,y1,x1,y1);
        NEIGHBOR(x5,y1,x1,y2);
        NEIGHBOR(x5,y1,x2,y1);
        NEIGHBOR(x5,y1,x4,y2);
        NEIGHBOR(x5,y1,x5,y2);
        NEIGHBOR(x5,y2,x1,y1);
        NEIGHBOR(x5,y2,x2,y2);
        NEIGHBOR(x5,y2,x3,y1);
        NEIGHBOR(x5,y2,x4,y1);
        TARGET(x5,y1);
        TARGET(x5,y2);
    };
}

instance inst_wildfire_mdp__5_2 {
    domain = wildfire_mdp;
    non-fluents = nf_wildfire_mdp__5_2;
    init-state {
        burning(x5,y1);
        burning(x5,y2);
        out-of-fuel(x5,y2);
    };
    max-nondef-actions = 1;
    horizon = 40;
    discount = 1.0;
}